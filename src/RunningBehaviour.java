/**
 * Used to define the different behaviours via which
 * someone can run.
 */
public interface RunningBehaviour {

    void run();
}


// Class -> Dog
// ClassA extends (B extend (A extends Object))
// Class (extends Object)
// wer should onluy inherit 2 classes
// if they share the same abstraction
// (attributes) inherently